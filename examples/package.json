{
  "name": "bullet-sdk-examples",
  "private": true,
  "workspaces": [
    "node",
    "web"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
